const { test } = require("@playwright/test");

test.describe("Authenticated flows", () => {
  test.skip("requires dedicated test accounts for admin/teacher/student", async () => {
    // Intentionally skipped until test credentials are available.
  });
});
