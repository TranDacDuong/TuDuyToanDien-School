# Project Rules & Customizations

## Git Commit & Push Rule
- Sau khi hoàn thành code / chỉnh sửa file thành công, tự động commit và push thẳng lên nhánh `main` (`git add ... && git commit -m ... && git push origin main`).
